# KRYN execution ledger — 2026-09-25

This is the single requirement/evidence ledger for the current product goal. `Implemented` means code exists; `mechanism checked` means a narrow test ran; `qualified` requires representative installed use. Recheck evidence after a relevant edit or merge. This candidate branch starts from `main` at `1e79244`; the latest published prerelease is v0.1.10 from `e9c90ff`. The owner currently has a private, experimental 64K candidate at `8ec3f11` installed; the repository release profile remains 49K and no public tier has changed. The target Mac is an M4 Max, 48 GB, macOS 26.6.2. Sustained trials must record the actual power state. Public releases, tags and version bumps are paused. A read-only audit found no newer public release or tag; older releases were left unchanged.

| Requirement | Current implementation / evidence | Remaining acceptance gate | Status |
|---|---|---|---|
| Agent-ready repository | [PR #4](https://github.com/PranavMishra28/kryn/pull/4) merged at `8cb55bd`; `make check` and clean package smoke passed locally and in CI; independent diff review found no issue | Format/static commands, focused runbooks and fresh-agent navigation/use | In progress |
| Native experience | `tools/localai.py`, `tools/kryn_tui.tsx`, `setup/opencode.template.json`; v0.1.10 candidate installed, external-project TUI opened with Agent and 2/2 tools connected. [Candidate qualification](evals/history/2026-09-25-v010/qualification.json). Saved Build remains equivalent; Browse/Reviewer/Audit stay visible because hiding saved read-only roles would silently switch them to Agent on TUI continuation. Chrome 154 launched and handled a click through pinned Playwright; v0.1.10 doctor accepted it. | Safely migrate saved role selection, then reduce the picker to Ask/Plan/Agent; terminal/GUI continuation and denied writes; full permission-toggle roundtrip | In progress; GUI permission roundtrip unqualified |
| Public installation | [v0.1.10 prerelease](https://github.com/PranavMishra28/kryn/releases/tag/v0.1.10) from `e9c90ff` is published. Anonymous asset download and SHA256 passed; public install, rollback/update, uninstall/reactivation and repair of a missing owned launcher passed on the owner's existing installation, followed by guarded inference, deep doctor and external-project TUI launch. [Evidence](evals/history/2026-09-25-v010/qualification.json). | Prove non-owner clean install and offline recovery | In progress; existing-owner release path qualified narrowly |
| Model and context | [Release profile](setup/accepted-profile.json): Qwen3.5-9B-6bit, 49,152 context, 8,192 output. [Private tier evidence](evals/history/2026-09-25-context-tiers/qualification.json): 49K and 64K thinking near-limit synthetic retrieval passed; 96K and 128K thinking near-limit requests stopped on host pressure. The owner kept an experimental 64K installation; three large agent starts and one short Agent workflow also stopped on pressure, although the short task’s edited artifact passed 2/2 frozen checks. The private 64K source `4506b32` passed package checks, guarded protocol smoke and doctor; an actual rollback from the prior f275b29 64K install restored matching 49K source `8d39c67`. The private 64K source `e9cc4b0` passed package checks and doctor; an actual rollback from it restored matching `4506b32` 64K source, and reinstall plus guarded protocol smoke passed. Private source `069084e` passed package checks and doctor. Private source `bce6887` passed `make check`, committed-source package smoke, installed package check, a guarded native child-agent/compaction probe, and doctor at 65,536 context with normal pressure. An actual rollback from `bce6887` restored matching `069084e` at 64K; reinstall, guarded protocol smoke and doctor passed. Private source `67901e8` passed source/package checks, installation, three guarded compaction/restart stages and post-trial doctor at 65,536 context. Current private source `8ec3f11` passed source/package checks, installation, guarded protocol smoke, a fresh retained-prefix compaction and doctor at 65,536 context. Its immediate rollback target is `67901e8` at 64K; exact-source rollback and clean non-owner installation remain unqualified. The exercised 49K baseline is retained farther back. oMLX offers opt-in KV compression, but an [upstream 4-bit warm-cache wrong-answer report](https://github.com/jundot/omlx/issues/3690) makes cold/warm correctness testing essential before using compression to seek 96K or 128K. | Reliable sustained Agent completion at 64K and representative quality; latest matching 49K rollback/reinstall passed, with no public tier change on synthetic results | Primary work; experimental 64K is not qualified |
| Context and evidence | Native structured compaction plus an unreleased `tools/context_capsule.mjs` candidate reads bounded current Git/file facts, marks changed checkpoint state, and replays a bounded private record of the first and latest three user requests after compaction. Offline restart/stale-file regressions pass. In private Task12, two real compactions and a restart completed, but the first checkpoint falsely denied any task. The first-compaction hook now supplies the durable request in an offline regression; a later guarded protocol retry passed and a first native compaction completed. That summary retained goals/criteria/next action but promoted assistant proposals to user decisions and omitted no-checks-run status; updated guidance then passed a narrow installed planning fixture with two native compactions, a restart and changed-file detection. The first summary still omitted explicit no-checks-run status, which the recalled turn and second checkpoint recovered. Sustained coding/UI continuity is unqualified. Task12d completed one resumed coding turn and native checkpoint after an initial timeout; 29 focused tests and 2 frozen API checks passed, but its checkpoint falsely said checks were still running. The observed-check ledger now accepts the exact non-masking `cd … && test 2>&1` form; a narrow installed compaction/restart retest passed and marked the prior pass stale on restart. Long coding/UI check continuity remains unqualified. Fresh Task12e completed two native compactions and a restart during coding/UI, but the second checkpoint mislabeled completed UI edits as active, labeled model choices as user decisions, and repeated unverified success claims. A bounded historical handoff tail is now supplied beside the checkpoint in source and the private 64K installation. Long private requests now retain both beginning and end, and latest requests precede lower-priority file excerpts under the 8K prompt cap. Offline repeated-compaction and Unicode-overflow regressions preserve late acceptance text and mark omitted middle/evidence; arbitrary omitted-middle criteria still require native-history review. A fresh planning fixture completed three native compactions across restarts, but was pinned no-tools. A tool-enabled filter stage timed out after edits/tests; its close checkpoint mislabeled model edits as user Decisions. A resumed Ask turn flagged this and a fresh read-only Reviewer found that generated filter tests copied production logic. Two subsequent frozen filter stages completed native compaction at 64K, but both checkpoints again classified agent implementation choices as user Decisions; a conservative provenance check now flags Decision bullets without a verbatim user anchor. A newly installed 64K planning fixture completed two native compactions across a restart on AC power, but its Ask turn wrongly treated a historical Plan-mode note as invalid after the mode changed; tool-enabled semantic continuity remains unqualified. Task12k added four compactions across saved-session coding/UI work, but repeated summaries mislabeled agent choices as user Decisions; the first lost failed-test status and the fourth omitted fresh UI edits. Source now shows the observed-check ledger to Ask/Plan as well as coding roles. The read-only-role prompt correction was installed privately at `e9cc4b0`: a narrow Ask probe made no shell attempt and kept a failed check, but its native summaries still promoted observations into user Decisions. Semantic continuity remains unqualified. These are partial signals, not a passed long coding/UI continuity gate. | Prove every explicit criterion, decision, failed check and next action survives repeated compaction/restart; independent long coding/UI acceptance | Primary work; a false first checkpoint failed semantic continuity despite structural completion |
| Application/research quality | [v0.1.10 candidate trials](evals/history/2026-09-25-v010/qualification.json) timed out on Task06. The [private context Task12 trial](evals/history/2026-09-25-context-candidate/qualification.json) passed its two frozen API checks, but both ten-minute Build stages timed out. Independent Chrome passed edit, upload, export, reload and 503 recovery, then failed exact project filtering and a 375 px viewport. A fresh Task12e browser probe passed 7/9 checks, including import, persistence, export, 503 recovery and narrow layout; filter and edit failed. It waited for the filter response, so the filter failure is not a UI timing artifact. A fresh broad Reviewer timed out; a focused Reviewer found the Save routing bug but proposed an incorrect fix. A later independent Chrome run passed 7/7 functional checks, while generated backend tests still error and unescaped row rendering caused executable DOM injection in a separate owned Chrome probe. No native Browse call completed. | Repair tests and row rendering, then independently rerun browser/API/accessibility and broad review | Failed overall; latest frozen Task12 grade PARTIAL (2/2 API only) |
| Process control | A [post-release Task06 probe](evals/history/2026-09-25-v010/qualification.json) was interrupted after four broad `killall python` attempts against a disposable server; no browser flow was verified. The narrow direct-command guard is merged on `main` in [PR #10](https://github.com/PranavMishra28/kryn/pull/10); a [private installed candidate](evals/history/2026-09-25-guard-candidate/qualification.json) refused one direct shell call before execution. A [new Task12c trial](evals/history/2026-09-25-context-candidate/qualification.json) exposed three compound multiline `pkill -f` calls that bypassed the direct guard; the owned trial was interrupted. Candidate source now scans simple chained/multiline command positions; focused tests and an installed one-call native probe pass, sustained validation pending. | Exercise the expanded guard in sustained work; assess remaining process-signaling paths without claiming whole-process isolation | In progress; public v0.1.10 still permits broad signals |
| Bounded learning | `tools/improvement.py`, `tools/learning.py`; incident capture and paused optional worker | Reproduce, isolate, reject, promote, monitor and rollback with held-out evidence; no unproved uplift claim | Mechanism checked; benefit unproved |
| Benchmark comparison | `evals/bench.py`, `evals/external_requests.py`; one Requests trial timed out and failed | Predeclared same-model harness comparison, model challenger comparison, practical official benchmark subset, distribution of verified outcomes | Open; no parity or uplift claim |
| Release and sustain | [v0.1.10 qualification](evals/history/2026-09-25-v010/qualification.json): public artifact/update/rollback and guarded restart passed on the owner Mac; one installed Agent task-02 PASS in 173 seconds with normal pressure and zero swap growth; two UI runs timed out. Initial menu-bar alert required one manual orphan-runtime recovery. | Multiple compactions and restart in a long coding/UI task, 49K–128K decision, clean install/rollback, sustained work, security and recovery gates | Paused; no public release proposal until every gate passes |

Next dependency order: finish the private context candidate and prove requirement retention through multiple native compactions and restart; run and independently verify a sustained coding/UI task; decide the largest useful context tier; the latest 49K rollback/reinstall passed, while clean non-owner installation remains open. Only then propose a public release. Advance the ledger only with linked tests, actual run artifacts, and current source revision. Private transcripts and machine state stay outside this repository. The [private Task12 evidence](evals/history/2026-09-25-context-candidate/qualification.json) records a false first checkpoint, two timed-out Build stages, and two independent browser failures; follow-up Task12c exposed a compound-shell process guard bypass and was stopped. Task12d reached a passing partial backend artifact after a resumed turn. Task12e added two native compactions and a restart across coding/UI but failed semantic checkpoint and independent browser gates. Its repair retry stopped at resource preflight without sending a prompt. On an earlier installed 64K build, Task12f passed a narrow no-tools planning/restart fixture; Task12g timed out on a filter edit, then a fresh Reviewer found its model-written tests copied the implementation. A measured repeat of the same frozen filter stage completed in 184 and 146 seconds under normal pressure with no swap growth, but both model-written test sets copied the filter predicate rather than exercising HTTP; the second run also changed an existing test. Independent HTTP probes passed nonempty exact filtering, while the full frozen Task12 grade still failed. A prompt-only test-quality instruction did not resolve this and was reverted. The prior f275b29 private 64K installation had a one-step matching 49K rollback receipt and passed a guarded protocol smoke; an actual rollback to that 49K source later passed. Its two-compaction planning/restart fixture remained no-tools and exposed a historical-mode interpretation error. The full UI/review/handoff gate remains open. This is a failed workflow gate, not a successful long application trial.

The exact `f275b29` private 64K Task12k trial ran eight native stages for 1517.558 active seconds (~25.3 minutes) on AC power with normal pressure and no observed swap growth. Four native compactions and saved-session restarts occurred, but semantic continuity failed: checkpoints promoted agent choices to user Decisions, lost failed-check status in the first summary, then omitted fresh UI changes. Two Build stages and a broad read-only review timed out. A focused Reviewer found the new-Save routing bug but proposed an incorrect fix. Two narrow coding corrections followed; the final independent Chrome browser run passed 7/7 functional checks, including Save/Edit, 503 retry, import, filter, export and responsive views. Generated backend tests still errored (9/19 newer, 17/19 older); frozen grade is PARTIAL with 2/2 API checks. The latest UI uses unescaped row values in `innerHTML`, a DOM injection that a separate owned Chrome probe executed; the functional script missed it. This is a failed long coding/UI and 45–60 minute sustained gate; it does not establish an input-window limitation or qualify the 64K tier. A small source change now exposes observed failed checks to Ask/Plan handoffs. It passed an installed controlled-failure probe over three native compactions and two Ask restarts, but the last checkpoint still gave a stale Next Move; full semantic and long-workflow validation remain open. [Detailed evidence](evals/history/2026-09-25-context-candidate/qualification.json).

A separate installed `4506b32` mechanism probe executed one intentionally failing unittest and retained its exit-1 failure across three native compactions and two Ask restarts. All stages ran on AC power at normal pressure with zero swap growth. The first Ask answer incorrectly equated baseline tests with Task02 acceptance; explicit correction fixed its next answer, but the third checkpoint kept a stale next action. This narrow result does not qualify Task12 or the 64K tier. [Evidence](evals/history/2026-09-25-context-candidate/qualification.json). Current disk free space was about 37 GiB. Three already-downloaded 27B challenger variants are retained, so model comparison need not download another copy; earlier 27B trials hit host pressure and none is qualified for the owner profile.

The fresh installed `4506b32` Task12m trial used one saved session over Plan, backend Build, recovery Build and Ask. The corrected Plan compacted, but its checkpoint promoted an agent correction to `Decisions` and repeated completed planning. Backend Build timed out at 488.140 seconds; an explicitly steered recovery timed out at 308.115 seconds; the Ask restart timed out at 188.179 seconds after attempting an unavailable shell tool. The frozen API grader passed 2/2, but the agent's 14-test suite had two failures caused by malformed generated test inputs, and UI/browser/review acceptance did not run. All stages were on AC power with normal pressure and no swap growth, even with Cursor and Chrome open. This shows 64K remained memory-safe for these stages, not that it is reliably useful for the full workflow. A role-specific prompt fix now avoids telling Ask/Plan to rerun shell checks. The owner installed private source `e9cc4b0` at 64K after offline and package checks; doctor passed. A fresh controlled-failure fixture then completed three native compactions across two restarts with zero swap growth. Ask made no shell attempt and a later answer correctly rejected false user Decisions, but both later checkpoints still promoted observations into `Decisions` and the final Next Move mislabeled baseline tests as Task02 acceptance. Source now masks unsupported Decision bullets only in the model-facing native checkpoint copy and retains raw history. Current private source `069084e` passed offline/package checks, installed and passed doctor; its fresh three-compaction fixture kept `Decisions: (none)` and the failed check across two Ask restarts, but the live masking path was not exercised because no false Decision summary was produced. The third answer also misquoted checkpoint/user text. Semantic checkpoint quality and sustained coding/UI remain unqualified. [Detailed evidence](evals/history/2026-09-25-context-candidate/qualification.json).

Private source `bce6887` corrects native child-session provenance and prevents post-cancellation trial compaction. A fresh installed 64K Build probe invoked native Explore after one mistaken skill call, then completed its parent turn and compaction in 77.577 seconds. The child tracker had no user-request continuity record; the parent tracker retained the real user prompt. The run was on AC power, normal pressure level 1, with zero observed swap growth. Package check and doctor passed after the run. Exact-source rollback to the previous 64K package, reinstall, guarded protocol smoke and doctor then passed. This validates narrow provenance and existing-owner recovery paths, not repeated restart quality, sustained coding/UI, or the 64K useful-tier gate. [Evidence](evals/history/2026-09-25-context-candidate/qualification.json).

A fresh installed `bce6887` Task12n trial completed Plan and one native compaction, but the checkpoint falsely marked completed planning as unfinished. The pinned OpenCode compaction kept the latest exchange in `recent-context` and sent only an older prefix to the summarizer; wire evidence showed no assistant/tool content in that request. The resumed backend turn timed out after 608.125 seconds and eight tool errors, including repeated rejected trailing-`&` server starts. A focused recovery timed out after 248.223 seconds with no tool errors. Frozen API checks passed 2/2, but the task remained PARTIAL with no UI, browser, review or final handoff. All three stages ran on battery at normal pressure with zero observed swap growth. Source now gives an empty-prefix native compaction a deterministic truthful checkpoint that directs continuation to retained recent history, and stops treating `Decisions: (none)` as a claim. Offline checks passed. A fresh installed planning fixture then completed three native compactions across two restarts; its first checkpoint stayed truthful about unseen recent work and Ask recalled completed planning, but the second model checkpoint again mislabeled four implementation observations as user Decisions. An exact offline replay masked those four rows and the next Ask correctly reported no user Decisions. The original first-stage driver marked a false routing failure because this plugin-supplied checkpoint made no model request; corrected driver replay verified the route coverage, while the raw failed record remains. Full coding/UI qualification stays open. [Evidence](evals/history/2026-09-25-context-candidate/qualification.json).

The installed `8ec3f11` private 64K build completed a fresh Task12 Plan turn and plugin-supplied first checkpoint in 70.239 seconds. The summary precisely reported no visible assistant/tool work in the selected older prefix while native recent-context retained the completed Plan exchange; route coverage and the resource guard passed. One Plan tool call still tried unavailable `execute`. A separate read-only Task02 turn completed with ordinary model compaction. Both short probes ran on battery at normal pressure with zero observed swap growth. This validates the wording and audit mechanism, not the full workflow. [Evidence](evals/history/2026-09-25-context-candidate/qualification.json).

The [paired private 64K variant screen](evals/history/2026-09-25-context-candidate/variant-screen.json) used identical fresh Task12 API fixtures and prompts. Fast (thinking budget 0) and default (3072) both passed the two frozen automatic API checks but timed out at five minutes with tool errors and no completed turn. Memory pressure stayed normal and swap did not grow; both ran on battery. The installed private 64K profile stays in place, and this screen does not qualify either variant, the context tier, or the long coding/UI gate.

Source now binds current-file retrieval and unsupported-Decision masking to the SHA-256 of the native compaction event's summary. The pinned OpenCode 2.0.10 projection adds framing newlines around that summary; the matcher removes exactly those framing bytes before comparing. A later ordinary message with a forged full checkpoint wrapper cannot displace the matching native summary in focused regressions across restart. Existing saved checkpoints without this hash retain a legacy wrapper path. The installed probe below covers this narrow mechanism; sustained coding/UI acceptance still requires retesting.

The [installed private checkpoint-binding probe](evals/history/2026-09-25-context-candidate/checkpoint-binding.json) on source `b754fdc` completed two Task02 Ask turns, two native compactions and a native server restart. Each private tracker hash matched the exported native summary; the resumed answer kept all six criteria and accurately said code and tests were unrun. Both stages had zero tool errors, normal pressure and no swap growth on battery. This narrowly validates the binding path, not the long coding/UI gate. A direct client-only deployment during setup temporarily mismatched OpenCode's old plugin reference and doctor refused launch; restoring the prior client recovered doctor, and the wheel's transactional `kryn install` activated the candidate with package check and post-trial doctor passing at 64K. A source regression now makes the direct deployment refuse such a mismatch before activation. The new deployment guard still needs installed validation.

In a fresh AC-powered Task12 implementation stage, a narrower API-only turn completed in 219.812 seconds with 29 tool calls, zero tool errors, one native compaction and 2/2 frozen API checks. It did not run manual HTTP or UI acceptance. Its final report and checkpoint nevertheless claimed zero failed/unrun checks while an earlier `pytest` command failed because the module was unavailable; a separate seed inspection also errored. The checkpoint promoted agent implementation choices to user Decisions. A 38.187-second Ask restart correctly said no user Decisions were verified and flagged the failed/stale ledger contradiction, but could only see a hashed failed-check reference and proposed retrying pytest. Source now retains a canonical runner label, exit code and bounded diagnosis for recognized checks across restart, with legacy tracker migration and no raw command/output copy. This source fix is offline-tested and awaits an installed continuity retest; the full Task12 grade remains PARTIAL.

The follow-on [private 64K Task12 trial](evals/history/2026-09-25-context-candidate/task12r-sliced.json) covered 12 guarded stages, nine native compactions, saved-session restarts, an independent Chrome run and a new-session review/handoff. Its measured active stage time was 47.277 minutes, with normal sampled pressure and no observed swap growth. Frozen API checks passed 2/2; after multiple operator-steered fixes, Chrome passed the specified create, edit, exact filter, import, export, retry and narrow-layout checks. The overall frozen grade remains PARTIAL. Three coding stages timed out, generated tests need cleanup, and native summaries repeatedly treated old work as current or model choices as user Decisions. The fresh reviewer made path errors and proposed a filter change contrary to the task contract. This is neither a continuous autonomous pass nor evidence that 64K is the largest reliably useful tier. Source now labels checkpoint work and next actions as potentially stale when a newer native exchange was retained, points read-only roles at the exact project root, and gives a bounded retry hint for oversized writes. These changes require installed semantic retesting. Public release and promotion remain paused; the owner keeps a private 64K installation.

The private 64K Task09 endurance baseline completed ten sequential turns and checkpoints, but turns 9 and 10 edited before reading current rows. A bounded file head-and-tail retrieval fix is being compared privately; see the retained baseline evidence in the PR branch. This is not a sustained coding/UI qualification.


The paired private 64K Task09 tail-retrieval run passed ten automatic checkpoints but failed the read-before-edit workflow gate (2/10 explicit pre-edit reads vs 8/10 baseline); the PR branch retains detailed evidence. A short Agent instruction to read the live target file after restart is under private comparison.

